---
name: Security

on: # yamllint disable-line rule:truthy
  deployment_status:
    branches:
      - gh-pages
  pull_request: {}

jobs:
  dast:
    name: DAST
    permissions:
      contents: read
      security-events: write
    uses: fabasoad/reusable-workflows/.github/workflows/wf-security-dast.yml@feat/security
    with:
      target-url: "https://fabasoad.com"
